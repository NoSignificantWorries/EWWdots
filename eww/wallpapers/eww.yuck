(defwidget wallpaper-picker[]
    (box
        :orientation "vertical"
        :class "wallpaper-picker"
        (label
            :text "Select Wallpaper"
            :class "title"
        )
        (literal
            :content (include "$HOME/EWWdots/scripts/wallpapers/wallpaper_thumbnails.sh")
        )
        (button
            :class "apply-button"
            :onclick "notify-send 'Test' 'Hello'"
            "Apply"
        )
    )
)

(defwindow wallpaper-picker
    :monitor 0
    :geometry (geometry
        :width 500
        :height 300
        :anchor "center"
        :title "Wallpaper picker")
    :stacking "fg"
    :resizeable false
    :namespace "wallpaper-picker"
    (wallpaper-picker)
)
